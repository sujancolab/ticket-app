import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Royal Machine Maintenance',
  webDir: 'dist'
};

export default config;
